# Daily Log: 2026-04-26

## Sessions

## Memory Maintenance

### Session (02:06)

**Context:** Shipping and merging Qualia Framework v4.2.0–v4.3.0 — 6 stacked PRs covering memory layer, self-healing, adversarial verification, and cron-runnable flush.

**Key Exchanges:**
- Built v4.3.0 phase 1 (PR #12): `/qualia-postmortem` self-healing skill, adversarial verifier flag, knowledge loader subdirectory support
- Built v4.3.0 phase 2 (PR #13): `bin/knowledge-flush.js` cron-runnable non-interactive flush — completes the memory loop end-to-end without needing a Claude session
- Fixed a bug where `logEvent` swallowed ENOENT when `~/.claude/` didn't exist
- Merged all 6 PRs (#8→#9→#10→#11→#12→#13) into main in correct dependency order
- Installed crontab entry for weekly Sunday 3AM flush

**Decisions Made:**
- Paused at 6 PRs rather than continuing to worktree parallelism — reviewer fatigue threshold, and worktrees need a clean base
- Merge order locked: #8→#13 sequential (each builds on previous)
- Cron schedule: `0 3 * * 0` (Sunday 3AM) for knowledge flush
- Deferred v4.3.0 phase 3 (worktree parallelism) and v4.4.0 (orphan cleanup, per-employee scoping) until the new pieces are battle-tested for a week

**Lessons Learned:**
- 6 stacked PRs is the practical ceiling before review quality degrades
- `logEvent` needs defensive directory creation — hooks fire in contexts where `~/.claude/` may not exist yet
- Cron flush correctly no-ops with `skipped/no-recent-daily-log` when the daily-log is empty — good defensive design
- The full memory loop is now: Stop hook → daily-log → cron flush → concepts/ → loader → builder reads before writing → postmortem catches misses

**Action Items:**
- Restart Claude Code session to load new hooks (Stop, git-guardrails)
- Use framework for ~1 week before building worktree parallelism
- Consider bumping `package.json` from 4.1.1 → 4.3.0 before npm publish
- v4.3.0 phase 3 queued: worktree-aware parallelism (shell scripts, port allocation, DB branch lifecycle)
- v4.4.0 queued: orphan skill cleanup, color drift, per-employee memory scoping (audit findings #4, #6, #7, #8)
- 11 of 12 original audit findings now closed; 1 remaining

### Session (02:29)

**Context:** Shipping and merging Qualia Framework v4.2.0 + v4.3.0 — 6 stacked PRs merged to main, cron installed, memory layer now live.

**Key Exchanges:**
- Built v4.3.0 phase 1 (PR #12): `/qualia-postmortem` self-healing skill, adversarial verifier flag, loader subdirectory support
- Built v4.3.0 phase 2 (PR #13): `bin/knowledge-flush.js` cron-runnable non-interactive flush — completes the memory loop end-to-end
- Merged all 6 PRs in order: #8 → #9 → #10 → #11 → #12 → #13 — main advanced from `dc4ca80` → `1f4d312` (+5,119 lines, 27 files)
- Installed crontab: `0 3 * * 0 node ~/.claude/bin/knowledge-flush.js` for Sunday 3 AM weekly flush
- Ran `qualia-framework doctor` — all checks pass. 28 skills, 8 agents, 9 hooks confirmed.

**Decisions Made:**
- Paused stack at 6 PRs to avoid review fatigue — worktree parallelism (phase 3) deferred until this stack is battle-tested
- Merge order was sequential (#8→#13) because each PR builds on the previous
- `package.json` version intentionally left at 4.1.1 — npm publish + bump to 4.3.0 deferred

**Lessons Learned:**
- `logEvent` in the Stop hook was swallowing ENOENT when `~/.claude/` didn't exist — needed a quick mkdir guard (caught by test suite)
- 6 PRs is the practical stacking threshold before cognitive overhead hurts reviewers
- Cron flush correctly skips with `{"event":"skipped","reason":"no-recent-daily-log"}` when no daily-log entries exist yet — first real flush will fire after a few sessions populate it

**Action Items:**
- Use framework for ~1 week to accumulate daily-log entries and validate the memory loop in practice
- v4.3.0 phase 3 next: worktree-aware phase parallelism (`bin/qualia-worktree.sh`, port-from-hash, Supabase branch-per-worktree)
- v4.4.0 queued: orphan skill cleanup, color drift, per-employee memory scoping (audit findings #4, #6, #7, #8)
- npm publish + version bump to 4.3.0 when ready to ship publicly
- Memory entry saved at `memory/v4.3.0-shipped-state.md` — future sessions will read this

### Session (02:31)

**Context:** LLM Wiki framework integration — inventory, CLAUDE.md fix, skill validation, cron scheduling, then ship.

**Key Exchanges:**
- User gave numbered tasks 1–4 with instruction to report after 1–2, then chain 3–4
- Cost flag raised on history deep-mine: 1,559 JSONL files / 694 MB would cost $250–400 via SDK — pivoted to free filesystem inventory instead (53 projects, 380 sessions catalogued)
- `/wiki-query` validated with voice agents query — returned 6 cited pages, named projects, MRR figures, gaps identified

**Decisions Made:**
- Free inventory-first approach over expensive SDK deep-mine — build manifest, then target 5–10 projects that matter (rationale: $250+ vs $0)
- CronCreate used for weekly wiki-lint but accepted 7-day auto-expire limitation — no permanent scheduler in Claude Code
- Vault shipped to master (not a deployed app, just a git repo — no Vercel/Supabase needed)

**Lessons Learned:**
- Claude Code CronCreate auto-expires recurring tasks after 7 days — cannot rely on it for permanent schedules. Need system crontab wrapper for true forever-weekly
- When user gives blanket "do whatever" permission, still flag costs over ~$5 and propose free alternatives before burning money (captured in `feedback_cost_flagging.md`)
- Runtime cron state files (`.claude/`) should be gitignored — they were cluttering git status

**Action Items:**
- Deep-mine the top 5–10 projects from `wiki/sources/claude-history-inventory.md` via `/wiki-history-ingest claude` (cost: $50–250 depending on scope)
- Revisit permanent weekly wiki-lint schedule if manual runs feel burdensome
- `raw/sessions/` will populate when SessionEnd hook fires on conversation close

### Session (02:52)



**Context:** Armenius.cy voice/text widget — fixing product search quality, widget formatting, and deploying category-based search

**Key Exchanges:**
- User wanted laptops under €800 but PrestaShop search returned RAM modules instead (RAM descriptions contain "laptop")
- Added `max_price`/`min_price` params to `searchProducts` webhook + ElevenLabs tool schema
- Discovered PrestaShop relevance ranking is poor — brand-specific queries work better than generic "laptop"
- User asked for widget formatting improvements and category filtering without having the PrestaShop API key handy
- Solved API key access by adding a `/listCategories` endpoint to the webhook itself (which already has the key as a Cloudflare secret)

**Decisions Made:**
- **Category filter over keyword search:** Mapped PrestaShop category IDs (Laptops=224, Desktops=221, Monitors=238, etc.) into a `category` enum param so the LLM passes `category="laptops"` instead of searching the word "laptop"
- **Brand search as fallback:** Updated system prompt to search by brand (ASUS, Lenovo, HP, Dell, etc.) when user asks for laptops, as a secondary strategy
- **Text-only formatting:** Product list rendering (bulleted cards, €-symbol conversion) applied only to text chat widget — voice agent left untouched to preserve conversational style
- **Filter €0 products:** Products with no price (hidden/out-of-stock) excluded from results

**Lessons Learned:**
- PrestaShop `/search` endpoint matches keywords in product descriptions, not just titles — causes irrelevant results (RAM for "laptop" query). Category-based filtering is the reliable path
- When you can't access an API key directly, you can add a discovery endpoint to the service that already has the key
- `display=[id,associations]` filter on PrestaShop API strips out the product list from category responses — need to fetch without display constraint
- ElevenLabs tool schema `description` field is how you teach the LLM when to use params — include trigger phrases like "under N", "less than N"

**Action Items:**
- Category IDs mapped: Laptops=224, Laptops(refurb)=438, Desktop=221, Monitors=238, Keyboards=239, Headsets=241, Headphones=292, Smartphones=287, Tablets=288, Printers=376, Gaming=316
- CI pipeline active: push to main → typecheck → wrangler deploy → health check
- Widget live at qualia-widgets.vercel.app, webhook at armenius-webhook.workers.dev

### Session (02:56)

**Context:** Armenius widget (qualia-widgets.vercel.app) — formatting overhaul, category-based product search, and deployment

**Key Exchanges:**
- User wanted better widget formatting → shipped right-aligned user bubbles, left-aligned agent cards, status pill, typing indicator, mobile responsive
- User didn't have PrestaShop API key handy → solved by adding `/listCategories` endpoint to the webhook itself (already has the key as Cloudflare secret)
- Category IDs discovered: Laptops=224, Desktops=221, Monitors=238, Smartphones=287, etc.
- User requested product listings show as bulleted lists with € pricing — but voice agent must stay untouched
- €0 phantom products were appearing → filtered out in searchProducts

**Decisions Made:**
- Category filter added to `searchProducts` with named enum (laptops/desktops/monitors/etc.) mapping to PrestaShop IDs internally — cleaner than exposing raw IDs to the LLM
- Product list formatting done purely in widget JS (not agent prompt) so voice agent stays conversational
- Brand-search fallback kept in system prompt alongside category param for specific model queries
- CI pipeline on `.github/workflows/deploy.yml`: typecheck → wrangler deploy → health check on every push to main

**Lessons Learned:**
- PrestaShop `display=[id,associations]` filter strips product lists from category responses — need to omit display constraint to get association data
- Products with price=0 are hidden/quote-only items in PrestaShop — always filter these out
- Widget text formatting (euro conversion, list rendering) should live in the widget layer, not the LLM prompt, to avoid affecting voice output

**Action Items:**
- **OPEN BUG:** "Start a Call" buttons not working — neither button triggers a call. Session ended before fix was applied. This is the immediate next task.
- Category filter is live but only tested with laptops — verify other categories (desktops, monitors, etc.) work correctly

### Session (03:09)

**Context:** Qualia ERP portal session — deployed Phase 25 performance optimizations (4 builder tasks: auth dedup, chat parallel writes, clients RPC, team-status RPC), applied live Supabase migrations, verified prod deploy. Then began `/qualia-design` on the projects gallery after user complained about ugly full-width single-column card layout.

**Key Exchanges:**
- Phase 25 build had a lint-staged race condition where Wave 1 parallel builders (T1, T3, T4) got bundled into a single commit `317234f` instead of separate commits. Code was correct, commit message misleading.
- Two Supabase RPCs (`get_per_client_project_stats`, `get_latest_session_per_profile`) were not in live DB — caught and applied before deploy.
- User flagged the projects gallery as "ugly" after the earlier UI tweak made stage columns single-column — cards became full-width sparse blocks.

**Decisions Made:**
- Deployed 4 optimization commits + 1 UI fix to prod via `vercel --prod` (commit `431e12e`)
- Applied both new RPC migrations directly to live Supabase (not just local)
- Design transformation started on projects gallery — diagnosis: single-column layout made cards too wide/sparse, needs redesign as compact horizontal rows

**Lessons Learned:**
- **lint-staged wave collision:** When multiple parallel builder agents commit simultaneously, lint-staged's stash/restore can bundle unrelated changes into one commit. Not harmful but creates misleading git history. Consider sequential commits for parallel waves.
- **Always check live Supabase before deploying RPC-dependent code** — migrations only exist locally until explicitly applied.
- **UI layout gotcha:** Switching from multi-column to single-column grid without redesigning the card component creates oversized sparse cards. Layout changes need matching card density adjustments.

**Action Items:**
- Complete the projects gallery design transformation (interrupted mid-analysis)
- Framework state files (OPTIMIZE.md, STATE.md, JOURNEY.md) are pre-v4 — need to decide on Option 3 (initialize v4 state) or stay legacy
- Dependabot reports 1 moderate vulnerability on master — separate track
- `state.js` will keep failing until v4 state is initialized

### Session (03:09)

**Context:** Executing Week 1 of the Qualia OS unification plan — wiring framework, ERP, and memory repos together based on a deep-research report.

**Key Exchanges:**
- Reviewed a previous strategic plan and identified 3 tactical mistakes: (1) proposed rewriting qualia-memory as an API when it's already a working Obsidian wiki, (2) proposed a new `.qualia/project.json` when `tracking.json` + `STATE.md` already do the job, (3) ignored the deep-research report's actual tool recommendations (Promptfoo, Langfuse, LiteLLM, n8n, Presidio)
- Corrected plan: stop building new products, start **wiring** existing repos with the report's "Balanced Stack" mapped onto 4 existing repos

**Decisions Made:**
- Deep research report stored at `~/.claude/knowledge/deep-research-2026-04-26.md` (not in framework repo or Obsidian vault)
- `tracking.json` extended with `client_id` + `framework_version` instead of creating a new identity file — backwards compatible (null = behave as today)
- Memory MCP built as **zero-dep** stdio JSON-RPC server (~120 lines) to match framework's zero-dependency design philosophy
- Memory MCP is **read-only by design** — writes only via humans + SessionEnd hooks
- Memory MCP wired globally in `bin/install.js` (same pattern as `next-devtools`), not per-project `.mcp.json`
- Path traversal guard added to Memory MCP (security)

**Lessons Learned:**
- Framework already had `project_id`, `team_id`, `git_remote`, `client` fields — only `client_id` and `framework_version` were actually missing. Always read before assuming gaps
- ERP report contract audit found 3 concrete mismatches: `client_id` and `framework_version` not accepted by ERP, and `session_id` sent but stored as generic `metadata` instead of indexed column
- ERP fix was ~30 LOC + 1 migration — small delta when the audit doc spells it out precisely
- lint-staged in ERP auto-formatted files on commit — no manual intervention needed

**Action Items:**
- Merge framework PR #14 BEFORE shipping ERP PR #87 to prod (field dependency)
- Run Supabase migration after ERP #87 merges (`npx supabase db push` or dashboard SQL)
- Run `npx supabase gen types` to sync `types/database.ts` from live schema
- Bump framework to v4.2.0 + CHANGELOG after both framework PRs (#14, #15) merge
- Week 2 priorities: Promptfoo CI template + `/qualia-eval` command, Langfuse on Railway, LiteLLM gateway in front of OpenRouter
- Week 3: n8n on Railway for lead pipeline (website form → Presidio scrub → ERP `/api/v1/leads`)

### Session (03:32)

**Context:** Finalizing the LLM Wiki (Karpathy-style second brain) in `~/qualia-memory/` — validation, shipping, Obsidian configuration, and graph visual overhaul.

**Key Exchanges:**
- Fawzi asked how the memory system works non-technically → explained the 3-loop model: auto-capture on session close, `/qualia-recall` to pull, `/wiki-update` to push
- Fawzi asked "is this how the Karpathy thing works?" → confirmed it's Karpathy's raw→wiki→schema pattern with Ar9av's slash commands + Cole Medin's auto-capture hook combined
- Recommended 4 Obsidian plugins: Templater (already installed), Linter, Tag Wrangler, Web Clipper (browser extension)

**Decisions Made:**
- CronCreate weekly `/wiki-lint` set but will auto-expire after 7 days (Claude Code limitation) — accepted as manual-when-needed rather than building a system crontab wrapper
- Skip Smart Connections / Copilot for Obsidian / Dataview — redundant with `/wiki-query` and Bases
- Graph view palette aligned to existing `vault-colors.css` brand palette (cyan=wiki, coral=analysis hubs, pink=entities, amber=sources, green=clients)

**Lessons Learned:**
- Claude Code `CronCreate` recurring tasks auto-expire after 7 days — don't rely on them for permanent schedules
- Obsidian graph quality is 80% configuration (color groups, force tuning, noise filtering) not plugins — per-node CSS glow isn't possible without Extended Graph plugin
- Karpathy wiki pattern fails when humans are the bottleneck for maintenance — the SessionEnd auto-capture hook is what makes it survive past week 2
- Obsidian plugins can be installed headlessly by downloading release assets into `.obsidian/plugins/` and updating `community-plugins.json`

**Action Items:**
- After closing session: verify `~/qualia-memory/raw/sessions/2026-04-26.md` auto-capture appears (proof the loop is live)
- Try `/qualia-recall` from a real project tomorrow to test brain retrieval
- Run `/wiki-update` from Sakani or ERP to feel the "push" side
- Enable Linter "Lint on save" setting after Ctrl+R reload
- Walk through Tag Wrangler and align tags with `wiki/_meta/taxonomy.md`
- Consider installing Minimal theme for typography + overall Obsidian polish
- Consider Extended Graph plugin if animated edges/node glow desired

### Session (03:35)

**Context:** Qualia Framework v4.3.0 release — merged PR stack, installed cron, prepped and attempted npm publish.

**Key Exchanges:**
- Merged 6 stacked PRs (#8→#9→#10→#11→#12→#13) in correct order via `gh pr merge --merge --delete-branch`
- Reinstalled framework locally — doctor reports green, 28 skills, 8 agents, 9 hooks confirmed
- Installed weekly cron: `0 3 * * 0 node ~/.claude/bin/knowledge-flush.js` — dry-run passed with `skipped:no-recent-daily-log`
- Prepared release branch: bumped `package.json` 4.1.1→4.3.0, consolidated 5 `[Unreleased]` CHANGELOG blocks into one `[4.3.0] — 2026-04-26` section
- PR #16 opened, merged, main advanced to 4.3.0
- `npm publish` blocked on 2FA OTP — awaiting Fawzi's authenticator code

**Decisions Made:**
- PR #14 (ERP tracking: client_id + framework_version) held for 4.3.1 — ERP-side migration not confirmed ready
- Merge order respected stacking: #8→#13, no rebase conflicts
- Version jump 4.1.1→4.3.0 (skipping 4.2.x) to reflect the scope of changes across all merged PRs

**Lessons Learned:**
- 6 PRs is the practical stacking limit before reviewer cognitive load becomes a problem
- `npm publish` with 2FA requires interactive OTP — can't be fully automated in CI-less flow
- Stop hook won't fire until session restart after install — daily-log starts empty after reinstall

**Action Items:**
- [ ] Complete `npm publish --otp=<code>` with Fawzi's 2FA code
- [ ] Tag `v4.3.0` and push tag after publish succeeds
- [ ] Run fresh-HOME smoke test: `npx qualia-framework@4.3.0 install` + `doctor`
- [ ] Use framework for ~1 week before starting v4.3.0 phase 3 (worktree parallelism)
- [ ] Queued: v4.4.0 (orphan skill cleanup, color drift, per-employee scoping)

### Session (03:52)



**Context:** Session focused on the Qualia Memory vault (~/qualia-memory) — Karpathy-style LLM wiki infrastructure. Shipped earlier work, then pivoted to Obsidian tooling and graph visualization.

**Key Exchanges:**
- Explained how the memory system works in non-technical terms: auto-capture on session close, `/qualia-recall` to pull knowledge, `/wiki-update` to push from projects
- Confirmed this implements Karpathy's three-layer pattern (raw → wiki → schema) with Ar9av's slash commands and Cole Medin's auto-capture hook
- Recommended 4 essential Obsidian plugins: Templater (already installed), Linter, Tag Wrangler, Web Clipper (browser extension)
- Installed Linter and Tag Wrangler directly into `.obsidian/plugins/` via CLI
- Built custom graph coloring config (`graph.json`) with brand-aligned color groups matching `vault-colors.css`
- Added `graph-style.css` snippet for deep-space backdrop and glass controls
- Discovered Obsidian overwrites `graph.json` from memory when graph view is open — edits must be made with graph tab closed, then reload

**Decisions Made:**
- Skip Smart Connections / Copilot for Obsidian (duplicates `/wiki-query`, costs tokens)
- Skip Dataview (Bases replaces it)
- Skip Calendar/Kanban (ERP handles that)
- Minimal theme installed for clean typography
- Graph color scheme: coral red = analysis hubs, cyan = wiki/concepts, pink = entities/people, amber = sources, violet = sessions, green = clients, blue = projects, gold = finances, orange = research

**Lessons Learned:**
- Obsidian holds graph config in memory and flushes to disk — external edits to `graph.json` get overwritten if graph view is open. Must close graph tab before editing, then reload
- `kepano/obsidian-skills` are agent skills (not plugins) — teach Claude to write better Obsidian markdown
- Obsidian graph nodes render to canvas, so per-node CSS glow isn't possible without Extended Graph or 3D Graph plugin

**Action Items:**
- Tomorrow: close this session → check `~/qualia-memory/raw/sessions/2026-04-26.md` for first auto-capture proof
- Try `/qualia-recall Andreas` or `/qualia-recall voice agents` to test recall
- Run `/wiki-update` from a real project (Sakani, ERP) to test the push flow
- Enable Linter "Lint on save" in Obsidian settings
- Use Tag Wrangler to align tags with `wiki/_meta/taxonomy.md`
- Consider installing Obsidian Web Clipper browser extension for `_raw/` ingestion

### Memory Flush (03:57)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (04:09)

**Context:** Qualia Framework v4.2.0–v4.3.0 merge, release, and npm publish session.

**Key Exchanges:**
- Merged 6 stacked PRs (#8→#9→#10→#11→#12→#13) in order to `main`, advancing from `dc4ca80` → `1f4d312` (+5,119 lines, 27 files)
- Installed weekly cron: `0 3 * * 0 node ~/.claude/bin/knowledge-flush.js` — smoke-tested with `--dry-run`
- Bumped `package.json` from 4.1.1 → 4.3.0, consolidated 5 `[Unreleased]` CHANGELOG blocks into one `[4.3.0]` release
- PR #16 (release/v4.3.0) merged. `npm publish` blocked on 2FA OTP — Fawzi needs to run manually
- Hasan proposed adding Microsoft's Markitdown CLI as framework dependency with auto-convert hook — reviewed and counter-proposed

**Decisions Made:**
- PR #14 (ERP tracking) held for 4.3.1 — ERP-side migration not ready
- v4.3.0 tag + npm publish deferred until Fawzi provides OTP code
- Markitdown: rejected Hasan's auto-convert-on-session-start + install.js approach. Approved concept as opt-in `/qualia-ingest` skill only (~150 LOC, no hooks, no install changes)
- Next milestones queued: v4.3.0 phase 3 (worktree parallelism), v4.4.0 (orphan skills, color drift, per-employee scoping)

**Lessons Learned:**
- Claude Code's Read tool handles PDFs natively now — Markitdown's unique value is DOCX/PPTX/XLSX/audio only
- Auto-conversion hooks that run every session are an anti-pattern (same class of issue as v4.1.0 audit finding #11 "invisible knowledge files")
- 6 stacked PRs is the practical limit before reviewer cognitive load degrades — pause and merge before stacking more

**Action Items:**
- Fawzi: run `npm publish --otp=<code>` to complete 4.3.0 publish
- Fawzi: run `git tag v4.3.0 && git push origin v4.3.0` after publish
- Scope `/qualia-ingest` SKILL.md as follow-up PR (4.3.1 or v4.4.0)
- Reply to Hasan: green-light the idea, reject the auto-hook architecture, propose opt-in skill instead
- Use framework for ~1 week before starting worktree parallelism (v4.3.0 phase 3)

### Session (04:09)



**Context:** Fawzi was configuring employee workflows, homepage UI, and client management features in the Qualia portal.

**Key Exchanges:**
- Confirmed Moayad's morning cron tasks (research + blog post) fire Mon–Fri at 5 AM UTC automatically, with idempotent dedup
- Clarified that `/clients` (full CRUD) and Control → Clients tab (read-only summary) share the same `clients` table but serve different purposes
- Proposed 3 options for Moayad's remaining morning routine time: LinkedIn outreach, CRM hygiene, and inbox triage — recommended Options 1 + 2

**Decisions Made:**
- Removed "Other" as a clock-in option — users must clock in against a project (commit `f1014dd`)
- Added framework update popup (`qualia-framework@latest install`) gated to admin+employee only, dismissed via localStorage key
- Added Delete Client button to `/clients/[id]` detail page with confirmation dialog (action already existed, just lacked UI)
- Added project logo circles to homepage "Projects in Flight" rows, threaded `logo_url` through both admin and employee data paths
- Created two manual tasks for Moayad: Finish Milestone 1 (09:30–12:30) and Finish Milestone 2 (13:30–17:00) for Qualia Solutions project

**Lessons Learned:**
- `deleteClientRecord` action existed but had zero UI callers — always check for orphaned server actions when users report missing features
- The framework update popup uses a date-stamped localStorage key (`qualia-framework-update-2026-04-26`) — bump the date suffix to re-trigger after future releases
- Milestone 2 phases aren't defined yet in `project_phases` — task description tells Moayad to plan M2 phases first before executing

**Action Items:**
- Awaiting Fawzi's pick of 2–3 morning routine items for Moayad's "First 3 hours" preset button (LinkedIn outreach / CRM hygiene / inbox triage)
- Wire the "First 3 hours" clock-in preset once selection is made

