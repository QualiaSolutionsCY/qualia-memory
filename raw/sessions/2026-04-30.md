# Daily Log: 2026-04-30

## Sessions

## Memory Maintenance

### Session (03:29)

**Context:** Production deployment fix for ai.underdogsales.com (claude-memory-compiler project)

**Key Exchanges:**
- Vercel production deployment had failed ~11h prior with `cacheComponents` feature flag errors related to `"use cache"` in `next.config.ts`
- Investigation showed the config already had `cacheComponents: true` correctly set — failure was due to stale Vercel build cache, not a code issue
- Local build and `npx tsc --noEmit` both passed clean

**Decisions Made:**
- Deployed directly via `vercel --prod` rather than investigating the stale cache further — pragmatic fix since code was already correct

**Lessons Learned:**
- Vercel build cache can cause phantom failures even when `next.config.ts` is correctly configured — if `"use cache"` directive fails with `cacheComponents` errors but the flag is present, suspect stale build cache rather than code issues
- Always verify local build + tsc before assuming a Vercel failure is a real code problem

**Action Items:**
- Monitor next few deploys to confirm the stale cache issue doesn't recur
- Consider adding `vercel --force` flag if cache-related failures happen again (forces clean build)

### Session (04:05)

**Context:** Qualia ERP portal — calendar, task management, project assignment cleanup, and a critical planning-sync bug fix across the fleet.

**Key Exchanges:**
- Fawzi reported seeing Moayad's tasks in his admin inbox — traced to a stale `view-as-user-id` cookie with no visible banner (banner gate fails when cookie points to invalid profile ID, making `viewAsName` null)
- Fawzi reported "Sync says 16 milestones but frontend doesn't update" on Sakani — root cause was a CHECK constraint mismatch: parser produces `'complete'`/`'pending'`/`'planned'` but DB only allows `'not_started'`/`'in_progress'`/`'completed'`/`'skipped'`. Upsert errors were silently swallowed, counter incremented regardless.

**Decisions Made:**
- Soft-remove (not hard delete) 23 active assignments to Done/Archived/Canceled projects — follows existing `removed_at` pattern
- Added `toDbPhaseStatus()` normalizer in `lib/planning-sync-core.ts` — maps all parser statuses to the 4 DB-allowed enum values. Global fix, not Sakani-specific.
- Sakani M13 ("Closed 2026-04-25") manually set to `skipped` since it's gated on merchant account

**Lessons Learned:**
- **Silent upsert failures are deadly** — always check `error` from Supabase upserts before incrementing success counters. The toast lied because the code assumed success.
- **View-as impersonation cookie bug:** if the cookie references an invalid/deleted profile, `viewAsName` is null → banner hidden, but `getEffectiveUser` still impersonates. Need to fix: show banner (or clear cookie) when profile lookup fails.
- **Pre-push hook + .gitignore conflict:** `.planning/` was gitignored but `tracking.json` was already tracked — hook couldn't update it. Fixed by un-ignoring `tracking.json` and `STATE.md`.
- Parser doesn't recognize "Closed" keyword in JOURNEY arc tables — future Sakani syncs will overwrite M13 back to `not_started`

**Action Items:**
- Fix `normalizeArcStatus` in `lib/planning-parser.ts` to map "Closed" → `skipped` (5-line fix, prevents M13 regression on next sync)
- Add persistent "Viewing as X" banner that also handles invalid/stale cookie IDs (clear cookie or show warning)
- Investigate SWR cache key in QualiaTasksView — doesn't include impersonated user ID, could serve stale data from previous view-as session

### Memory Flush (04:21)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:24)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:25)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:45)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:01)

FLUSH_OK - Nothing worth saving from this session

### Session (14:35)

**Context:** New client intake form built for Thomas Dennis (DV Corporate / Vivastreet companion site) — adult content platform

**Key Exchanges:**
- User wanted a new intake form like existing DawaDose/BioPharma patterns but for an adult escort profiles website (viva-demo repo)
- Required: access code gating (516278), budget tiers (under £5k / £5-15k / £15-30k / £30k+), no dashes in copy, Resend email integration
- Auto-send email to thomas.dennis@dvcorporate.com from info@qualiasolutions.com with link and code

**Decisions Made:**
- Pink/rose accent (#EC4899) chosen for the adult content vertical
- 3-step form flow: Your Team → The Platform → Budget and Timeline
- Tailored fields for companion site specifics: profile types, age/ID verification, location search, subscriptions, messaging, moderation, Vivastreet integration, payment processors (Stripe/CCBill/Segpay/crypto)
- Budget in GBP (£) matching the client's UK market
- Followed existing Panther/DawaDose intake pattern (Zod validation, Resend dual-send, 5/hr rate limit)

**Lessons Learned:**
- Intake forms follow a repeatable pattern across qualiasolutions.net: code-gated entry → multi-step form → Zod-validated API → Resend to info@ + personal Gmail
- Files always: `pages/{Name}Intake.tsx`, `api/{name}-intake.ts`, plus wiring in `App.tsx`, `loadingConfig.ts`, `rateLimit.ts`

**Action Items:**
- Changes left unstaged — needs commit and push
- Monitor for Thomas's form submission response
- Check Resend delivery confirmation for the outreach email

### Memory Flush (15:45)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

